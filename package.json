{
  "name": "game-reviews",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "clean": "rm -rf .nuxt .output dist node_modules",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "dev": "nuxt dev",
    "generate": "nuxt generate && touch dist/.nojekyll",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@nuxt/eslint": "^0.6.0",
    "@nuxt/ui": "^2.18.7",
    "heroicons": "^2.1.5",
    "nuxt": "^3.13.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
